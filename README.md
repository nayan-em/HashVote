# HashVote

## Install

To run the flask server - 
```
cd backend
pip install flask
python3 main.py
```

To start the frontend, run this in a seperate terminal -
```
cd frontend
npm install
npm start
```
